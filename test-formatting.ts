// Intentionally bad formatting to test hooks
export const     testFunction     =    (  a:number,b:string   )=>   {
        console.log(   "This has bad formatting"  )
    return    a+    b.length
}

   const unused   =   42